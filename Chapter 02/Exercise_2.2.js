const myName = "Ascendant Alf";
const myAge = 27;
const coder = true;
const message = `Hello, my name is \"${myName}\". I am ${myAge} years old and it is ${coder} that I can code Javascript`; 
console.log(message);

/* I used escape (backslash) on the quotes
next to my name, so that the quotes would
appear around my name in the log itself */